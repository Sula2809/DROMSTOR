import { HeaderMain } from "@/components/shared/HeaderMain/HeaderMain";
import { HeaderNav } from "@/components/shared/HeaderNav/HeaderNav";

export const Header = () => {
  return (
    <header className={``}>
      <HeaderMain />
      <HeaderNav />
    </header>
  );
};
